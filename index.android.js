//place code here for android
import React from 'react';
import { Text, AppRegistry } from 'react-native';

const Abs = () => (
        <Text>Some Text</Text>

);

//Render it to the device
ReactNative.AppRegistry.registerComponent('albums', ()=> Abs);